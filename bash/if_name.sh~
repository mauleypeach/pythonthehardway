#!/bin/bash
# 

echo -n "Hello what's you name? ... > "

read -r name

if [ -z $name  ];
then
    echo -n "Please enter your name... >"
    read -r name
else
    if [ -n $name ];
    then
	echo "Greetings $name, it's nice to meet you"
    fi
fi


